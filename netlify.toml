[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# Fallback SPA: envía todas las rutas al index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
